{
  "name": "crimson-wars",
  "version": "0.1.0",
  "private": true,
  "description": "Web co-op arena shooter inspired by Crimsonland",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check:encoding": "node scripts/encoding-guard.js",
    "db:local:up": "docker compose -f docker-compose.mysql.yml --env-file .env.mysql.local up -d",
    "db:local:down": "docker compose -f docker-compose.mysql.yml --env-file .env.mysql.local down",
    "db:local:migrate": "npm run migrate:mysql -- --truncate",
    "migrate:mysql": "node scripts/migrate-files-to-mysql.js",
    "typecheck": "node --check public/js/cw-menu-react.js && node --check public/js/client-core.js && node --check public/js/client-net-input.js && node --check public/js/client-render.js && node --check public/js/client-fx.js && node --check public/js/client-i18n.js",
    "build": "npm run typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "express": "^4.21.2",
    "mysql2": "^3.22.3",
    "ws": "^8.18.0"
  }
}
